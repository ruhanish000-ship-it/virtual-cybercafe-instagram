# Optional music

The automation generates its own original royalty-free instrumental by default.

To replace it, add exactly one licensed file here using one of these names:

- `custom_music.mp3`
- `custom_music.wav`
- `custom_music.m4a`

Use only music you created or have permission to reuse commercially. Music from a streaming subscription is not automatically licensed for business Reels.
